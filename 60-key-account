public Boolean isKeyAccount(Account a) {
    //code here

    switch on a {
        when(a.Industry == 'Banking') {
            if(a.AnnualRevenue >= 600000) return true;
                
        }
    }
}