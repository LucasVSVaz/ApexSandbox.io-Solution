public Map<String, List<Case>> casesByType(List<Case> cases) {
    //code here

    Map<String, List<Case>> getMap = Map<String, List<Case>>();

    for(Case c : cases){
        if(c.Type != null){
            getMap.put(c.Type, new List<Case>{c});
        }
    }

    return getMap;
}