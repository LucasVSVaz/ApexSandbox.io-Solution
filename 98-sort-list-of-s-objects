public List<Account> getAccounts(List<Account> accounts) {
    // code here

    if(accounts == null){
        return null;
    }

    List<AccountWrapper> acctWrapper = List<AccountWrapper>();

    for(Account acct : accounts){
        acctWrapper.add(acct);
    }

    acctWrapper.sort();

    for(Integer i = 0; i < accounts.size(); i++) {
        accounts[i] = acctWrapper[i].acc;
    }

    return accounts;

}


// write extra class here 
public class AccountWrapper implements Comparable{

    public Account acct;

    public Account (Account actt){
        this.acct = acct;
    }

    public Integer compareTo (Object compareTo){
        AccountWrapper compareToAcct = (AccountWrapper)compareTo;
        if (annualRevenue == compareToAcct.annualRevenue){
            return 0;
        } 
        if(annualRevenue > compareToAcct.annualRevenue){
            return 1;
        }
        return -1;
    }
}
