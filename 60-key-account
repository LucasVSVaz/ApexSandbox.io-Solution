public Boolean isKeyAccount(Account a) {
    //code here

    switch on Account {
        when(Account.Industry == 'Banking') {
            Account.AnnualRevenue >= 600000 return true
        }
    }
}