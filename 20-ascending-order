public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    //code here
    Boolean checkA = ((a <= b) && (b <= c) && (c >= b));

    if(checkA){
        return true;
    }
    return false;
}