public Map<String, Decimal> industrySummary(List<Account> accounts) {
    //code here
    Decimal sum = 0.0;
    String name;

    Map<String, Decimal> getMap = new Map<String, Decimal>();

    for(Account acct : accounts){
        sum += acct.AnnualRevenue;
        name = acct;
        getMap.put(name + acct.Industry, sum);
    }

    return getMap;
}