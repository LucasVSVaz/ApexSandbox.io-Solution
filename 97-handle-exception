public String divide(Integer a, Integer b){
    //code here

    Integer result = 0;

    try{
        result = a / b;
        return String.valueOf(result);
    } catch (Exception e){
        throw new AuraHandledException('Divide by 0');
        return error.body.message;
    }
}